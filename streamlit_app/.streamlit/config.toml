# Streamlit configuration - no theme colors specified to allow full auto-detection

[theme]
# No colors defined - Streamlit will auto-detect system theme and use built-in light/dark
primaryColor = "#3b82f6"
font = "sans serif"

[client]
showSidebarNavigation = true

[server]
headless = true
enableCORS = false
port = 8501
