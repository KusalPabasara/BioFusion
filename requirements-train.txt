# Dependencies for training (train_model.py).
# The Streamlit app uses streamlit_app/requirements.txt instead.
#
# For a CUDA GPU, install torch/torchvision from the PyTorch index first, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# then: pip install -r requirements-train.txt
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=9.0.0
scikit-learn>=1.3.0
kagglehub>=0.3.0
